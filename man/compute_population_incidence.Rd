% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zimphia_incidence.R
\name{compute_population_incidence}
\alias{compute_population_incidence}
\title{Compute the population-level implied incidence per posterior draw}
\usage{
compute_population_incidence(draws, pop)
}
\arguments{
\item{draws}{Tibble with one row per posterior draw, columns \code{alpha},
\code{beta}, \code{gamma}.}

\item{pop}{Tibble with one row per subject, columns \code{age}, \code{X1}, \code{weight}.}
}
\value{
Tibble with one row: \code{incidence_median}, \code{incidence_q2.5},
\code{incidence_q97.5}, expressed per person-year.
}
\description{
For each draw, evaluates h(a_i | x_i) on the ZIMPHIA analysis tibble, then
weights by \code{weight} and averages to yield an incidence rate (per
person-year). Returns the posterior median and 95\% credible interval.
}
\examples{
draws <- tibble::tibble(alpha = 5, beta = 0, gamma = 1.5)
pop <- tibble::tibble(age = c(20, 30), X1 = c(0, 1), weight = c(1, 1))
compute_population_incidence(draws, pop)
}
