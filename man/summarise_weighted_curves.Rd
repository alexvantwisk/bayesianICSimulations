% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_survival.R
\name{summarise_weighted_curves}
\alias{summarise_weighted_curves}
\title{Summarise weighted survival curves across replicates}
\usage{
summarise_weighted_curves(
  sims,
  grid = NULL,
  estimator = c("parametric"),
  weight_metric = c("cv", "kish_ess"),
  include_covariate = TRUE
)
}
\arguments{
\item{sims}{List of data.frames from load_sims_from_dir(), all in same design cell}

\item{grid}{Numeric vector of time points for prediction (default NULL = auto)}

\item{estimator}{Character, estimation method (currently only "parametric")}

\item{weight_metric}{Character, either "cv" (coefficient of variation) or
"kish_ess" (Kish effective sample size)}

\item{include_covariate}{Logical, if TRUE fit ~ X1, else fit ~ 1 (default TRUE)}
}
\value{
List with components:
\describe{
\item{rep_df}{Long data.frame with columns: rep_id, t, S, weight_metric_value,
X1 (if include_covariate=TRUE)}
\item{sum_df}{Data.frame with pointwise summaries: t, S_mean, S_med,
S_q50l, S_q50u, S_q95l, S_q95u, X1 (if stratified)}
\item{meta}{List with n_total, n_converged, dropped_reps, grid}
}
}
\description{
Fits parametric log-logistic AFT models to each replicate using icenReg,
predicts survival curves, and computes pointwise summaries (mean, median,
quantiles) across replicates. Also computes weight inequality metrics.
}
\examples{
\dontrun{
results <- summarise_weighted_curves(
  sims = sims,
  weight_metric = "cv",
  include_covariate = TRUE
)
}

}
