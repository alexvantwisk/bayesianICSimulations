% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misspec_simulation.R
\name{simulate_survival_data_weibull}
\alias{simulate_survival_data_weibull}
\title{Simulate interval-censored data under a Weibull DGM}
\usage{
simulate_survival_data_weibull(
  n,
  params,
  weight_type = c("none", "low", "high")
)
}
\arguments{
\item{n}{Sample size.}

\item{params}{List of parameters as produced by get_default_params().}

\item{weight_type}{Weighting scheme. One of "none", "low", or "high".}
}
\value{
Same tibble shape as \code{simulate_survival_data()}: columns X1, A0,
A_event, visit, L, R, status, weight.
}
\description{
Mirror of \code{simulate_survival_data()} (log-logistic) with the inverse-CDF
sample swapped for Weibull. The fit-side log-logistic model becomes
misspecified — this is the point of the misspecification scenario (Task F).
}
\examples{
set.seed(1)
params <- get_default_params()
simulate_survival_data_weibull(n = 50, params = params, weight_type = "low")
}
