% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_helpers.R, R/viz_survival.R
\name{compute_true_loglogistic}
\alias{compute_true_loglogistic}
\title{Compute true log-logistic survival/hazard functions}
\usage{
compute_true_loglogistic(
  t,
  alpha,
  gamma,
  beta = 0,
  X1 = 0,
  type = c("S", "H", "h")
)

compute_true_loglogistic(
  t,
  alpha,
  gamma,
  beta = 0,
  X1 = 0,
  type = c("S", "H", "h")
)
}
\arguments{
\item{t}{Numeric vector of time points}

\item{alpha}{Baseline scale parameter}

\item{gamma}{Shape parameter}

\item{beta}{AFT coefficient (default 0)}

\item{X1}{Covariate value (default 0)}

\item{type}{Character: "S" (survival), "H" (cumulative hazard), "h" (hazard)}
}
\value{
Numeric vector of function values at t

Numeric vector of function values at t
}
\description{
Calculates the true survival, cumulative hazard, or hazard function for a
log-logistic distribution with AFT parameterization.
}
\section{Log-Logistic Formulas}{

\itemize{
\item AFT parameterization: \code{alpha_i = alpha * exp(beta * X1)}
\item Survival: \code{S(t) = 1 / (1 + (t/alpha_i)^gamma)}
\item Cumulative hazard: \code{H(t) = -log(S(t))}
\item Hazard: \code{h(t) = (gamma/alpha_i) * (t/alpha_i)^(gamma-1) / (1 + (t/alpha_i)^gamma)}
}
}

\keyword{internal}
