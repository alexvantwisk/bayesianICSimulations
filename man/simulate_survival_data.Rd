% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_survival_data}
\alias{simulate_survival_data}
\title{Simulate interval-censored survival data}
\usage{
simulate_survival_data(n, params, weight_type = c("none", "low", "high"))
}
\arguments{
\item{n}{Sample size.}

\item{params}{List of parameters as produced by get_default_params().}

\item{weight_type}{Weighting scheme. One of "none", "low", or "high".}
}
\value{
A tibble with one row per subject containing:
X1 (covariate), A0 (entry age), A_event (absolute event age or admin limit),
visit (absolute visit age), interval bounds L and R (DURATIONS since entry),
status (0 right-censored, 3 interval), and analysis weight.
}
\description{
Simulate interval-censored survival data
}
\examples{
params <- get_default_params()
set.seed(1)
dat <- simulate_survival_data(50, params, "low")
head(dat)
}
