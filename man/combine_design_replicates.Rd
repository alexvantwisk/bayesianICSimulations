% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zimphia_design_variance.R
\name{combine_design_replicates}
\alias{combine_design_replicates}
\title{Combine per-replicate ZIMPHIA design posteriors via Rubin's rules}
\usage{
combine_design_replicates(replicate_summaries)
}
\arguments{
\item{replicate_summaries}{Tibble combining per-replicate \code{summary.rds}
files written by \code{\link[=fit_zimphia_design_replicates]{fit_zimphia_design_replicates()}}. Must include
columns \code{variable}, \code{mean}, \code{sd}, \code{replicate}.}
}
\value{
Tibble with one row per parameter: \code{variable}, \code{m} (number of
replicates), \code{q_bar} (combined point estimate), \code{w_bar} (mean
within-replicate variance), \code{b} (between-replicate variance of means),
\code{t_total} (Rubin total variance), \code{se_total} (\eqn{\sqrt{T}}),
\code{ci_lower}, \code{ci_upper}.
}
\description{
For each parameter, compute the design-aware total variance
\deqn{T = \bar{W} + (1 + 1/m) B,}
where \eqn{\bar{W}} is the mean within-replicate posterior variance
(\code{mean(sd^2)}) and \eqn{B} is the between-replicate variance of
posterior means (\code{var(mean)}). The approximate 95\\% credible interval is
\eqn{\bar{Q} \pm 1.96\sqrt{T}}, where \eqn{\bar{Q}} is the mean across
replicates of the per-replicate posterior mean.
}
\details{
This is the design-aware combination of Bayesian fits over survey-design
replicate weights as described by Beaumont and Bocci (2008), using the
classical Rubin (1987) multiple-imputation total-variance decomposition.
}
\examples{
\dontrun{
reps <- list.files(
  "mcmc_outputs/zimphia_design_replicates",
  pattern = "summary\\\\.rds$", recursive = TRUE, full.names = TRUE
)
all_summ <- purrr::map_dfr(reps, readRDS)
combine_design_replicates(all_summ)
}
}
