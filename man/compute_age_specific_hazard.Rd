% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zimphia_incidence.R
\name{compute_age_specific_hazard}
\alias{compute_age_specific_hazard}
\title{Compute posterior age-specific hazard h(a | x) for a log-logistic AFT model}
\usage{
compute_age_specific_hazard(draws, ages, x = 0)
}
\arguments{
\item{draws}{Data frame of posterior draws with columns \code{alpha}, \code{beta}, \code{gamma}.}

\item{ages}{Numeric vector of ages to evaluate.}

\item{x}{Numeric scalar covariate value (e.g. 0 = male, 1 = female).}
}
\value{
Tibble with columns \code{age}, \code{hazard_mean}, \code{hazard_q2.5}, \code{hazard_q97.5}.
}
\description{
For each draw \verb{(alpha, beta, gamma)} and each age \code{a}, returns the hazard
\deqn{h(a | x) = (\gamma / \alpha_x) \cdot (a / \alpha_x)^{\gamma - 1} / (1 + (a / \alpha_x)^\gamma)}
with \eqn{\alpha_x = \alpha \cdot \exp(x^T \beta)}.
}
\examples{
draws <- tibble::tibble(alpha = c(5, 5), beta = c(0, 0), gamma = c(1.5, 1.5))
compute_age_specific_hazard(draws, ages = c(20, 30, 40))
}
