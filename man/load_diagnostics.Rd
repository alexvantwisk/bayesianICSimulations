% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_results.R, R/data_loading.R
\name{load_diagnostics}
\alias{load_diagnostics}
\title{Load all diagnostic files for a given method and sample size}
\usage{
load_diagnostics(method, n_obs, results_dir = "mcmc_outputs")

load_diagnostics(method, n_obs, results_dir = "mcmc_outputs")
}
\arguments{
\item{method}{Character string. Analysis method, either \code{"hmc"} or \code{"mh"}.}

\item{n_obs}{Integer. Sample size (typically 200, 2000, or 10000).}

\item{results_dir}{Character string. Path to results directory. Defaults to
\code{"results"} in current working directory.}

\item{verbose}{Print progress messages?}
}
\value{
Tibble with all diagnostics

A tibble with all diagnostics for the specified method and sample size.
Returns empty tibble if directory doesn't exist or no files are found.
}
\description{
Loads and combines all diagnostic files for a given MCMC method and sample
size combination. Includes progress reporting via \pkg{progressr}.
}
\examples{
\dontrun{
# Load all HMC diagnostics for n=200
hmc_diag <- load_diagnostics("hmc", 200)

# Check convergence
table(hmc_diag$max_rhat <= 1.01, hmc_diag$min_ess >= 400)
}

}
\keyword{internal}
