% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_survival.R
\name{plot_weighted_spaghetti}
\alias{plot_weighted_spaghetti}
\title{Plot ghosted spaghetti survival curves}
\usage{
plot_weighted_spaghetti(
  sum_df,
  rep_df,
  true_alpha = 5,
  true_gamma = 1.5,
  true_beta = -0.5,
  X1_dist = c(0.5, 0.5),
  which = c("S", "H", "h"),
  n_ghost = 30,
  seed = 1,
  palette = "viridis",
  title = NULL,
  show_covariate_curves = FALSE
)
}
\arguments{
\item{sum_df}{Summary data.frame from summarise_weighted_curves()}

\item{rep_df}{Replicate-level data.frame from summarise_weighted_curves()}

\item{true_alpha}{Numeric, true alpha parameter (default 5.0)}

\item{true_gamma}{Numeric, true gamma parameter (default 1.5)}

\item{true_beta}{Numeric, true beta parameter (default -0.5)}

\item{X1_dist}{Numeric vector c(P(X1=0), P(X1=1)) for marginal curve (default c(0.5, 0.5))}

\item{which}{Character, "S" (survival), "H" (cumulative hazard), or "h" (hazard)}

\item{n_ghost}{Integer, number of ghost replicates to plot (default 30)}

\item{seed}{Integer, random seed for ghost sampling (default 1)}

\item{palette}{Character, viridis palette name (default "viridis")}

\item{title}{Character, plot title (default NULL = auto)}

\item{show_covariate_curves}{Logical, if TRUE and data has X1, show separate
true curves for X1=0 and X1=1 (default FALSE)}
}
\value{
ggplot object
}
\description{
Creates a publication-ready plot with ghosted replicate curves, pointwise
summary ribbons, mean curve, and true population curve overlay.
}
