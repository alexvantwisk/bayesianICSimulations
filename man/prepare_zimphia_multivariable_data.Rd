% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zimphia_multivariable.R
\name{prepare_zimphia_multivariable_data}
\alias{prepare_zimphia_multivariable_data}
\title{Prepare a multivariable design matrix for the ZIMPHIA fit}
\usage{
prepare_zimphia_multivariable_data(base, indiv, covariates)
}
\arguments{
\item{base}{A data frame with at least \code{personid}, \code{L}, \code{R}, \code{X1}, \code{weight}.
This is the analysis tibble produced by \code{run_zimphia_analysis()}.}

\item{indiv}{A data frame with \code{personid} and the demographic columns named
by \code{covariates}. ZIMPHIA codings: \code{gender} (1=male, 2=female), \code{urban}
(1=urban, 2=rural), \code{age} (years).}

\item{covariates}{Character vector of covariate names to include. Recognised:
\code{"sex"}, \code{"urban_rural"}, \code{"age_band"}, \code{"wealth_quintile"}.}
}
\value{
A list with \code{data} (augmented tibble), \code{X} (numeric matrix N x K),
and \code{covariate_levels} (factor levels for reference encoding).
}
\description{
Joins demographic columns from the individual file onto the analysis tibble
produced by \code{run_zimphia_analysis()} and returns a model matrix \code{X} of width
\code{K} together with the augmented data frame.
}
