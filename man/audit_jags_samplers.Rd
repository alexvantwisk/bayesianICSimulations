% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jags_audit.R
\name{audit_jags_samplers}
\alias{audit_jags_samplers}
\title{Audit which JAGS samplers are assigned to each node}
\usage{
audit_jags_samplers(
  data,
  model_file = system.file("models", "loglogistic_interval.jags", package =
    "bayesianICSimulations"),
  n_chains = 1L,
  n_adapt = 100L
)
}
\arguments{
\item{data}{A list suitable for \code{\link[rjags:jags.model]{rjags::jags.model()}} (N, L, R, X, w, zeros).}

\item{model_file}{Path to the JAGS model file.}

\item{n_chains}{Number of chains to initialise. Default 1.}

\item{n_adapt}{Number of adaptation iterations. Default 100.}
}
\value{
A tibble with columns \code{node} and \code{sampler}.
}
\description{
Loads the JAGS model on a tiny dataset and returns the per-node sampler
assignment reported by \code{\link[rjags:list.samplers]{rjags::list.samplers()}}. Used to confirm whether
the project's MH baseline is actually slice-sampling rather than
adaptive Metropolis (see revision plan Task A).
}
\examples{
\dontrun{
dat <- list(
  N = 10, L = rexp(10, 1) + 1e-10, R = rep(1e11, 10),
  X = rbinom(10, 1, 0.5), w = rep(1, 10), zeros = rep(0, 10)
)
audit_jags_samplers(dat)
}
}
