| Image (file) | Purpose / why relevant | Placement | Must change | Nice to add |
|-----------|-----------|-----------|-------------|-----------|
| **R-hat ECDFs by sample size** (`fig1a_rhat_ecdf.png`) | Convergence diagnostic across samplers and n; BASIS diagnostic reporting. | **Appendix – Diagnostics** | Add vertical thresholds at 1.01 (and faint 1.005); annotate each facet with % chains ≤1.01; fix x-limits to 1.000–1.020; unify legend. | Thin line at ECDF=0.95; small inset with median R-hat by method and n. |
| **ESS distributions by scenario (ridges)** (`fig1b_ess_ridges.png`) | Scenario-wise mixing (ESS, not per second). | **Appendix – Diagnostics** | Clarify axis label (ESS vs ESS/s); mark medians; ensure log x-labels readable (1K, 3K, 10K); increase vertical spacing. | Order scenarios by median ESS; faint refs at 1K/3K/10K. |
| **Coverage of 95% CIs with MC bands** (`fig2_coverage.png`) | Accuracy vs nominal coverage for parameters. | **Main body (Ch. 4)** **→ β panel only**; full 3-parameter figure in **Appendix – Results** | Crop to β; keep grey 95%±2% band; slightly larger points; rotate scenario labels; caption that bars reflect MC uncertainty. | Add side strip with mean CI width; overall coverage summary strip. |
| **Mean bias of β₁** (`fig3a_bias.png`) | Bias of estimand; supports accuracy narrative. | **Appendix – Results** | Common y-limits; add MCSE error bars; ensure y=0 dashed line prominent; symbol mapping consistent across figs. | Text labels when bias ≥0.03; small table of absolute bias differences (HMC–MH). |
| **RMSE of β₁** (`fig3b_rmse.png`) | Accuracy/precision summary per scenario. | **Main body (Ch. 4)** | Common y-axis across facets; add % difference labels ((HMC–MH)/MH); legend/title consistent. | Light log ticks (0.03, 0.1, 0.3) while keeping linear scale; thin lines connecting n within method. |
| **Bias–RMSE trade-off for β₁** (`fig3c_bias_rmse_tradeoff.png`) | Integrates bias & RMSE; shows trade-offs. | **Appendix – Results** | Clarify horizontal bars (95% CI width); add gridlines; axes start at 0; unify shapes/colours. | Overlay faint iso-MSE curves; call-out panel for worst cells. |
| **ESS per second (violins)** (`fig4_ess_per_sec.png`) | Core efficiency metric (ESS/s) by n. | **Main body (Ch. 4)** | Identical y-limits (log) across facets; show median dots and labels; thinner outlines. | Inset with geometric-mean ESS/s ratio (HMC/MH) by n; annotate extreme outliers with scenario tags. |
| **Total runtime by scenario (violins)** (`fig5_runtime.png`) | Raw wall-time; heavy and redundant. | **Exclude** (superseded by speed-up heatmap). | — | If requested, move to Appendix and show medians only on log-scale. |
| **Relative runtime: MH vs HMC (heatmap)** (`fig5b_speedup_heatmap.png`) | Compact speed-up comparison across cells. | **Appendix – Results** | Make legend & tiles both show **x-fold speed-up** (not “log2”); add note “blue = HMC faster”. | Thin tile borders; per-facet geometric mean speed-up in strip label. |
| **Weighted survival curves across design cells** (`fig6_survival_cells.png`) | Validates data-generating mechanism & weighting vs truth. | **Appendix – Methods/Validation**; include **3×3 thumbnail** in **Ch. 3** | Downsample ghosts (α≈0.1); emphasise median curve; ensure red “true” line on top; add legend for weight regimes. | Add Turnbull/icenReg overlay for 1–2 cells; one representative 3×3 in Ch. 3. |
| **Agreement of posterior means (HMC vs MH)** (`fig7_means_agreement.png`) | Consistency check: posterior means align across samplers. | **Appendix – Results (consistency)** | Use identical x/y ranges per facet; show 45° identity line in black; add Pearson r (and slope/intercept) per panel; increase point α/jitter to reduce overplotting. | Colour by scenario or weight regime (legend); marginal rugs on axes. |
| **Credible intervals vs true value (by n & parameter)** (`figA1_cis_vs_truth.png`) | Visualises estimate/CI position relative to truth for both methods. | **Appendix – Results** | Reorder columns to n=200, 2000, 10000; fixed x-limits per parameter; reduce overplotting (dodge more, smaller markers); clarify that vertical dashed line is truth. | Encode non-coverage (open symbols) vs coverage (filled); light background band for target region if applicable. |
| **CI matrix by parameter × n × method** (`figA2_ci_matrix.png`) | Dense CI view per method with coverage symbol coding. | **Appendix – Results (supplement to A1)** | Ensure identical x-scales within each parameter row; add vertical truth line; improve label spacing; legend text “● covered / ○ under-covered”. | Order scenarios logically (censoring 0.1→0.5 within weight none→low→high); thin panel borders. |
| **Precision–coverage trade-off (CI width vs coverage)** (`figB1_precision_tradeoff.png`) | Shows trade-off directly; connects HMC–MH pairs. | **Main body (Ch. 4)** **after coverage panel** | Confirm x-axis uses **median** 95% CI width; keep dashed 95% target line; add faint connecting line for each pair; jitter slightly to avoid overlap; consistent shapes by n. | Add smoothing line per parameter to indicate overall trend; annotate pairs with average Δ-width and Δ-coverage. |
| **Global bias in coverage (heatmap)** (`figB2_coverage_bias_heatmap.png`) | Summarises deviation from 0.95 across all cells. | **Appendix – Results** | Centred, symmetric colour scale (e.g., −0.10 to +0.10); annotate tiles with values; clear legend “observed–0.95 (absolute %)”; reorder scenarios by weight then censoring. | Add row summaries (small bars) at right showing mean bias per parameter×n. |
| **Distribution of CI widths (violins)** (`figB3_ci_width_violin.png`) | Precision comparison independent of coverage. | **Appendix – Results** | Common y-scale across all facets; show medians (points) and IQR (thick bar); consider log scale if heavy tails at n=200. | Overlay Δ-median width (HMC–MH) per facet as text; add horizontal refs for practical equivalence thresholds. |